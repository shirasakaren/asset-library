import { Module } from '@nestjs/common';

// TODO(Part 4): open/acknowledge/resolve flow. Issues reuse the Comment table
// with kind=ISSUE on the top-level row.
@Module({})
export class IssuesModule {}
